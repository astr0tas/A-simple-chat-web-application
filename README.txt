==>> Establish the database first.

==>> To run the server app, open the terminal and change the directory to the folder and run this code: npm start (nodemon server-app.js).

==>> To run the client app, open the terminal and change the directory to the folder and run this code: npm start.